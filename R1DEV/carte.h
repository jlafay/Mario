#include "constante.h"

extern Configuration parametre;
extern Carte coordonee;

extern SDL_Surface *loadImage(char *nom);
extern void drawTile(SDL_Surface *img, int finalex, int finaley, int initialex, int initialey);
extern void entite(int x, int y, int type);
